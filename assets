
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>S. | Social Media Influencer & Educator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #ffb37b 0%, #E38445 100%);
      min-height: 100vh;
      font-family: 'Poppins', Arial, sans-serif;
    }
    .container {
      margin: 30px auto;
      background: #fff;
      border-radius: 25px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.15);
      max-width: 1000px;
      padding: 0;
      overflow: hidden;
      position: relative;
    }
    .hero {
      display: flex;
      flex-direction: column;
      min-height: 600px;
      position: relative;
      background: linear-gradient(90deg, #1F1F1F 50%, #fff 50%);
    }
    .navbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 35px 50px 0 50px;
      z-index: 10;
      position: relative;
    }
    .logo {
      font-size: 2rem;
      font-weight: bold;
      font-family: 'Poppins', Arial, sans-serif;
    }
    .logo .black { color: #1F1F1F; }
    .logo .orange { color: #E38445; }
    .nav-links {
      display: flex;
      gap: 30px;
    }
    .nav-links a {
      color: #1F1F1F;
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1rem;
      transition: color 0.2s;
    }
    .nav-links a.active,
    .nav-links a:hover {
      color: #E38445;
    }
    .lang-dark {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .lang-dark span {
      color: #1F1F1F;
      font-weight: 500;
    }
    .dark-toggle {
      width: 36px;
      height: 20px;
      background: #eee;
      border-radius: 10px;
      position: relative;
      cursor: pointer;
    }
    .dark-toggle::after {
      content: '';
      position: absolute;
      left: 3px;
      top: 3px;
      width: 14px;
      height: 14px;
      background: #1F1F1F;
      border-radius: 50%;
      transition: left 0.2s;
    }
    .hero-image-bg {
      width: 400px;
      height: 400px;
      border-radius: 0; /* Square shape */
      overflow: hidden;
      position: relative;
      z-index: 5;
      margin: 0 auto;
      display: block;
      box-shadow: 0 0 30px rgba(255 255 255 / 0.3);
      top: 60px;
    }
    .hero-image-bg img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      border-radius: 0; /* Square shape */
    }
    .hero-content {
      text-align: center;
      position: relative;
      margin: 30px auto 60px auto;
      max-width: 600px;
      color: #fff;
      z-index: 10;
    }
    .hero-content h2 {
      font-size: 1.5rem;
      font-weight: 700;
      margin: 0.4em 0;
      letter-spacing: 0.5px;
    }
    .hero-content button {
      background: #E38445;
      color: #fff;
      border: none;
      border-radius: 5px;
      padding: 0.7em 1.8em;
      font-size: 1rem;
      font-weight: 600;
      margin-top: 1em;
      cursor: pointer;
      transition: background 0.2s;
    }
    .hero-content button:hover {
      background: #1F1F1F;
    }
    .social {
      position: absolute;
      left: 30px;
      top: 50%;
      transform: translateY(-50%);
      display: flex;
      flex-direction: column;
      gap: 18px;
      z-index: 10;
    }
    .social a {
      color: #1F1F1F;
      font-size: 1.15rem;
      text-decoration: none;
      transition: color 0.2s;
    }
    .social a:hover {
      color: #E38445;
    }
    .scroll {
      position: absolute;
      right: 30px;
      top: 50%;
      transform: translateY(-50%) rotate(90deg);
      color: #fff;
      font-size: 1rem;
      letter-spacing: 2px;
      z-index: 10;
      opacity: 0.8;
    }
    .splash {
      position: absolute;
      left: 0; top: 0; width: 100%; height: 100%;
      background: url('https://pngimg.com/uploads/splash/splash_PNG18817.png') center/cover no-repeat;
      opacity: 0.9;
      z-index: 1;
      pointer-events: none;
    }
    @media (max-width: 800px) {
      .container { max-width: 98vw; }
      .navbar { padding: 20px 15px 0 15px; }
      .hero-image-bg {
        width: 240px;
        height: 240px;
        top: 30px;
        margin-bottom: 10px;
      }
      .hero-content {
        max-width: 90vw;
        margin: 10px auto 40px auto;
      }
      .hero-content h2 {
        font-size: 1.2rem;
      }
      .hero-content button {
        font-size: 0.9rem;
      }
      .social, .scroll { display: none; }
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
</head>
<body>
  <div class="container">
    <div class="hero">
      <div class="navbar">
        <div class="logo"><span class="black">S</span><span class="orange">.</span></div>
        <div class="nav-links">
          <a href="#" class="active">Home.</a>
          <a href="#">About.</a>
          <a href="#">Skills.</a>
          <a href="#">Portfolio.</a>
          <a href="#">Contact.</a>
        </div>
        <div class="lang-dark">
          <span>En</span>
          <div class="dark-toggle"></div>
        </div>
      </div>
      <div class="social">
        <a href="#"><i class="fab fa-facebook-f"></i></a>
        <a href="#"><i class="fab fa-twitter"></i></a>
        <a href="#"><i class="fab fa-linkedin-in"></i></a>
      </div>
      <div class="hero-image-bg">
        <div class="splash"></div>
        <img src="https://images.unsplash.com/photo-1511367461989-f85a21fda167?auto=format&fit=facearea&w=400&h=400&facepad=2" alt="Profile" />
      </div>
      <div class="hero-content">
        <h2>Social Media Influencer & Educator</h2>
        <h2>Future Tech Innovator</h2>
        <button>GET IN TOUCH</button>
      </div>
      <div class="scroll">SCROLL</div>
    </div>
  </div>
</body>
</html>
